# 全局资产更新策略

- 更新项目中的模型、材质、贴图或其他美术资源时，必须先精确删除本次更新范围内的旧资源，再全量生成或导入新资源；不得依赖覆盖导入或 Reimport 保留旧设置。
- 删除范围必须限定在当前任务的目标资产及其明确废弃的依赖，保留所有无关资源。

# 窗口截图策略

- 截取应用程序、虚幻编辑器、客户端或其他工具的运行画面时，必须按目标窗口的实际边界进行窗口截图，不得使用整块桌面截图代替。
- 截图前必须恢复并显示目标窗口；若窗口最小化、隐藏或位于屏幕外，应先恢复并移动到可见区域，再按窗口边界截图。
- 多窗口验证时必须分别截取各个目标窗口，并使用清晰的文件名标明窗口或验证对象。

# 代码中文注释策略

- 新增或修改代码时，必须同步添加或更新准确的中文注释，便于人工审查和后续维护。
- 类、结构体、枚举、接口及模块入口必须说明其职责、适用范围和关键约束。
- 属性和成员变量必须说明其业务含义、生命周期、单位、有效范围、所有权或复制/持久化语义；含义相近的连续属性可以使用一条明确的分组注释。
- 方法和函数必须说明其用途、重要输入输出、副作用、调用前提及失败条件；网络 RPC、异步回调、生命周期方法和公共接口必须优先完整注释。
- 核心语句和关键分支必须说明设计意图，包括但不限于状态转换、事务边界、幂等处理、权限校验、网络通信、重试与超时、安全校验、资源加载以及异常回退。
- 注释必须描述“为什么这样做”以及必要的业务背景，不得仅将代码逐字翻译成中文，不得添加与实际行为不一致、无信息量或已经过期的注释。
- 修改代码行为后必须同步检查相关注释；删除或重构代码时必须同时清理失效注释，避免误导人工审查。

# Web 前端技术栈策略

- 本项目新建或重构的 Web 管理前端统一使用 Angular 22 和 TypeScript；不得新增原生 JavaScript、jQuery 或其他前端框架实现的生产页面。
- Angular 应用采用 standalone 入口和可测试的 TypeScript 模块；HTTP 调用、状态模型、组件职责和错误降级边界必须提供准确中文注释。
- Admin 前端源码统一放在 `Services/GuiyangMahjong.Admin/ClientApp`，生产构建产物输出到 `Services/GuiyangMahjong.Admin/wwwroot`。
- 修改 Admin 前端后必须执行 Angular 生产构建和 TypeScript 编译检查；不得手工修改构建产物代替源码修改。
- 后端监控依赖故障时，Angular 页面必须按数据源或面板独立降级，不得因单个请求失败导致整页白屏；陈旧数据必须显示来源、最后成功时间、数据年龄和陈旧阈值。
