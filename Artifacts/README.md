# 本地产物与阶段证据

`Artifacts` 默认用于可重建的构建、打包和验证输出，不是源码目录。各子目录必须明确属于以下一种策略：

- 可重建产物：加入 `.gitignore`，由构建或验证命令重新生成。
- 阶段审查证据：仅在已有审查链仍依赖时保留，并在后续迁入 `Evidence`。
- 发布制品：由 CI 制品库或镜像仓库保存，Git 只保留 manifest 和校验信息。

当前 `LinuxServer` 和 `WorkflowC` 属于可重建产物；`MonitoringWorkflowA` 暂按既有审查证据保留。未经专项迁移审查，不得批量删除其他子目录。
