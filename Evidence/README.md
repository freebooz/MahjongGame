# 长期审查证据

本目录用于保存必须跨构建保留、可关联需求或工单的验收证据。证据应不可变、可追溯，并与可重建发布产物分离。

每组证据应记录：生成时间、Git 提交、执行命令、环境、操作者、TraceId/工单（如适用）、校验和及对应审查文档。包含账号、IP、聊天、支付或其他敏感信息时必须脱敏并遵守访问控制与保留期限。

大体积日志、录像和二进制应进入受控对象存储，仓库只保留清单、摘要与校验和。

`Evidence` 不保存能够由构建或验证命令重新生成的完整产物；此类输出统一进入 `Artifacts`。本地临时截图、缓存和一次性验证目录统一进入 `Saved` 下对应分类目录。
